import 'package:flutter/material.dart';

class RedLampWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Icon(
      Icons.circle,
      color: Colors.red,
      size: 24,
    );
  }
}
