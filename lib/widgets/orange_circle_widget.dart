import 'package:flutter/material.dart';

class OrangeCircleWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Icon(
      Icons.circle,
      color: Colors.orange,
      size: 200, // サイズは調整可能
    );
  }
}
